using Microsoft.EntityFrameworkCore;
using Server.Models;

namespace Server.Data;

public static class SeedTier1to3
{
    public static void Seed(ModelBuilder builder)
    {
        // Add basic T1-T3 groups/attributes if necessary
    }
}
